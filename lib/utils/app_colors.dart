import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color(0xFF212121); // Dark Gray (For AppBar, primary buttons, etc.)
  static const blackColor = Colors.black;
  static const accentColor = Colors.orange; // Amber (For secondary buttons, highlights, etc.)
  static const background = Color(0xFFFAFAFA); // Light Gray (For backgrounds, cards, etc.)
}

// class AppColors2 {
//   static const primaryColor = Color(0xFF2E2E2E); // Deep Charcoal (For AppBar, primary buttons, etc.)
//   static const accentColor = Color(0xFFA48961); // Muted Gold/Bronze (For secondary buttons, highlights, etc.)
//   static const background = Color(0xFF424242);  // Dark Gray (For backgrounds, cards, etc.)
// }